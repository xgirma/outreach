export default function information(state = {}, action) {
  if (action.type === 'GET_INFORMATION_SUCCESS') {
    return {};
  }

  if (action.type === 'GET_INFORMATION_SUCCESS') {
    return {};
  }

  return state;
}
